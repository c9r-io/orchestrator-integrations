# GitHub Webhook Secret — copy this file and fill in your secret.
# Set when creating a webhook: Settings → Webhooks → Add webhook → Secret
apiVersion: orchestrator.dev/v2
kind: SecretStore
metadata:
  name: github-webhook
spec:
  data:
    webhook_secret: "<your-github-webhook-secret>"
