# LINE Channel Secret — copy this file and fill in your secret.
# Find it at: LINE Developers Console → Your Channel → Basic settings → Channel secret
apiVersion: orchestrator.dev/v2
kind: SecretStore
metadata:
  name: line-channel
spec:
  data:
    channel_secret: "<your-line-channel-secret>"
